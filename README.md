# Accelerate
Accelerate is an API response accelerator that uses caching to avoid the system accessing and computing data from the database repeatedly. That process is slow and can be expensive when scaled. 
